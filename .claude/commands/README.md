# Slash commands

Each `.md` file here is a slash command available in Claude Code sessions on
this repository. `edit-review.md` becomes `/edit-review`.

The longer editorial rules live in `.claude/skills/review-editing/SKILL.md`,
which Claude loads on its own whenever review copy is in play.
